"use client";
import { useState } from "react";
import { Field, TextArea, TextInput } from "@/components/form-fields";
import { SubmitButton } from "@/components/submit-button";
type Values={name:string;slug:string;description_de:string|null;logo_url:string|null;website_url:string|null;contact_email:string|null};
const slugify=(value:string)=>value.toLowerCase().replace(/ä/g,"ae").replace(/ö/g,"oe").replace(/ü/g,"ue").replace(/ß/g,"ss").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");
export function OrganizerForm({action,initial}:{action:(formData:FormData)=>void;initial?:Values}) { const [slug,setSlug]=useState(initial?.slug??""); const [touched,setTouched]=useState(Boolean(initial)); return <form action={action} className="flex max-w-xl flex-col gap-4"><Field label="Name" required><TextInput name="name" required defaultValue={initial?.name} onChange={event=>!touched&&setSlug(slugify(event.target.value))}/></Field><Field label="Slug (URL)" required><TextInput name="slug" required value={slug} onChange={event=>{setTouched(true);setSlug(event.target.value)}}/></Field><Field label="Beschreibung"><TextArea name="description_de" rows={5} defaultValue={initial?.description_de??""}/></Field><Field label="Logo-Bild URL"><TextInput name="logo_url" type="url" defaultValue={initial?.logo_url??""}/></Field><Field label="Website"><TextInput name="website_url" type="url" defaultValue={initial?.website_url??""}/></Field><Field label="Kontakt-E-Mail"><TextInput name="contact_email" type="email" defaultValue={initial?.contact_email??""}/></Field><SubmitButton>Speichern</SubmitButton></form>; }
