# Kopieren nach .env.local (oder auf admin/.env.local verweisen, siehe README)
# und mit echten Werten aus dem Supabase-Dashboard füllen. NIEMALS echte
# Werte hier oder in .env.local committen (siehe .gitignore).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
